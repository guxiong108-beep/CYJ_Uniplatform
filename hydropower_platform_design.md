# 水电站统一智能管理平台设计要点

> 版本：V1.0 · 定位：平台顶层设计纲要 · 适用阶段：筹建期 → 施工期 → 运营期

---

## 一、平台总体定位与设计原则

### 1.1 建设目标

构建覆盖水电站全生命周期的**两级统一智能管理平台**，实现对人员、设备、环境、电力系统的统一接入、统一感知、统一分析与统一管控，以AI能力驱动替代传统人工逐类对接模式，大幅降低集成成本，提升运营效率。

### 1.2 核心设计原则

| 原则 | 说明 |
|------|------|
| **AI驱动，而非规则驱动** | 以大语言模型（LLM）+ 领域模型替代人工编写的固定适配逻辑，自动完成数据理解与接口生成 |
| **最小必要数据** | 只提取各系统真正有用的关键字段，拒绝"对接100%只用10%"的低效模式 |
| **能力声明式接入** | 要求接入系统提供类MCP的能力描述，平台自动解析并访问，无需逐一调测 |
| **两级分层部署** | 中心级负责全局分析与决策，边缘级负责本地低时延处理与设备接入 |
| **工程实用优先** | 所有功能以现场可落地为标准，压缩冗余，专注高价值业务场景 |

---

## 二、平台两级架构设计

### 2.1 架构总览

```
┌─────────────────────────────────────────────────────┐
│                    中心级平台                         │
│  全局可视化 · AI根因分析 · 跨域协同 · 数据湖 · 策略下发  │
└───────────────────────┬─────────────────────────────┘
                        │  安全专网（5G/光纤）
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  边缘级节点A  │ │  边缘级节点B  │ │  边缘级节点C  │
│（变电站/地下  │ │（坝面/施工区）│ │（库区/大坝）  │
│  厂房就近部署）│ │              │ │              │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
  电力设备         手持/IoT设备       传感器/视频
  保护装置         人员定位           水文监测
  自动化系统        施工机械           闸门控制
```

### 2.2 中心级平台

**核心职责：**
- 汇聚所有边缘级上报的结构化数据，构建全站统一数据湖
- 运行全局AI分析引擎，执行跨系统、跨域的根因分析与趋势预测
- 提供统一的人机交互界面：大屏可视化、移动端、告警中心
- 负责平台策略配置、接入规则管理与能力声明注册中心维护
- 对接上级调度中心或集团管控平台（可选）

**关键能力：**
- 全站设备资产统一台账与拓扑视图
- 多系统事件关联分析与故障传播链推理
- LLM驱动的自然语言查询与分析报告生成
- 历史数据回溯与预测性维护模型训练

### 2.3 边缘级平台

**核心职责：**
- 就近部署于变电站、地下厂房等关键节点，实现**毫秒级本地响应**
- 承接各类终端设备的实时接入与协议适配
- 执行本地告警判断、联动控制与数据预处理
- 向中心级上报必要数据（非全量上报，经AI筛选后的关键字段）

**关键能力：**
- 本地实时计算与低时延告警（脱离中心级仍可独立运行）
- 多协议接入网关（IEC61850、Modbus、MQTT、OPC-UA等）
- 本地数据缓存与断网续传机制
- 边缘AI推理（轻量级模型，支持离线分析）

---

## 三、AI驱动的统一自动采集与适配机制

> 这是本平台区别于传统网管系统的核心创新点。

### 3.1 问题根源分析

传统集成模式的本质缺陷在于：每新增一类设备或系统，均需经历**需求调研 → 协议解析 → 接口开发 → 联调测试 → 上线验证**的完整周期，耗时数周乃至数月；设备厂商配合意愿和能力参差不齐，导致项目推进受制于外部因素。

此外，传统模式倾向于"全量对接"——为了避免遗漏，将对方系统的所有数据字段全部采集入库，实际使用率不足10%，产生大量无效数据处理与存储开销。

### 3.2 AI自动适配引擎设计

**（1）能力声明式接入（类MCP协议）**

要求所有接入系统（无论是电力保护装置、视频平台还是手持终端管理系统）提供标准化的**能力声明文档**，内容包括：

```yaml
# 能力声明示例（CapabilityManifest）
system:
  name: "××变电站保护装置管理系统"
  vendor: "××厂商"
  version: "2.3.1"

capabilities:
  - name: getDeviceStatus       # 能力名称
    description: "查询保护装置实时状态"
    protocol: IEC61850          # 接口协议
    endpoint: "/api/device/status"
    params: [deviceId, timeRange]
    returnFields:               # 声明返回字段及含义
      - field: "operStatus"
        meaning: "装置运行状态"
        unit: "enum: running/fault/offline"
      - field: "activePower"
        meaning: "有功功率"
        unit: "kW"
  
  - name: getAlarmEvents
    description: "查询告警事件列表"
    ...

dataKeywords:                   # 关键词标签，供AI理解系统领域
  - "继电保护"
  - "故障录波"
  - "开关量"
```

平台接收到能力声明后，由LLM自动解析，建立该系统的访问能力图谱，无需人工编写对接代码。

**（2）LLM驱动的关键数据自动提炼**

平台维护一套**监控意图模型**，描述各业务场景下"真正需要什么数据"。LLM在解析能力声明后，自动将监控意图与系统能力进行匹配，生成**最小必要数据采集策略**：

```
监控意图：变压器健康状态监测
  ↓ LLM匹配
必要字段：温度(绕组/油温)、负载率、局放量、告警状态
过滤字段：通信帧计数、版本号、调试日志、历史配置...（共47个无关字段不采集）
  ↓ 自动生成
采集接口：定时轮询 + 事件订阅双模式，采集频率自适应
```

**（3）自动数据格式建立与接口生成**

平台在完成能力理解后，自动执行以下流程：

- **数据模型自动生成**：根据能力声明中的字段定义，自动在数据湖中建立对应的数据模型（Schema），无需DBA手动建表
- **采集接口自动生成**：根据协议类型自动生成采集适配器代码或配置文件，支持REST、MQTT订阅、IEC61850 MMS、Modbus轮询等
- **数据标准化映射**：不同厂商的相同语义字段（如"设备状态"）自动归一化至平台统一语义层，消除异构命名差异

**（4）接口数据自动监听与动态适配**

对于已接入系统，平台通过**接口数据监听探针**持续感知数据结构变化：

- 当对方系统升级导致字段新增或格式变更时，平台自动检测并触发LLM重新解析
- 解析结果差异超过阈值时推送人工确认，差异较小时自动静默适配
- 全程保留适配变更记录，支持回溯审计

---

## 四、AI驱动的故障呈现与根因分析

### 4.1 分层告警体系

```
原始告警（来自各设备/系统）
    ↓ AI告警压缩与归并
归并告警（同类告警合并，噪声过滤）
    ↓ 关联分析引擎
事件链（多个告警关联为一个故障事件）
    ↓ 根因推理
根因结论 + 影响域分析 + 处置建议
```

传统平台告警风暴下，运维人员面对数百条告警无从下手；本平台通过AI归并压缩，将告警数量压缩至个位数的"事件"，每个事件附带完整的因果分析链。

### 4.2 根因分析引擎

**多源数据融合推理：**
- 融合电力系统告警、设备状态、视频异常、环境传感器、操作日志等多源数据
- 构建全站设备拓扑知识图谱，支持故障传播路径推理
- LLM负责将推理结果转化为自然语言说明，输出格式示例：

```
【根因分析报告】
故障时间：2025-08-21 14:32:17
根因设备：#2主变 35kV侧断路器（设备ID: CB-T2-35K）
根因判断：断路器操作回路断线（置信度 91%）
传播链路：操作回路断线 → 保护跳闸失败 → 差动保护越级动作 → 
          35kV母线失压 → 3台馈线开关连锁跳闸
影响范围：35kV I段母线全停，影响负荷约12.3 MW
处置建议：
  1. 立即到现场检查 CB-T2-35K 操作回路端子排 X3:7 接线
  2. 恢复前须完成继保人员现场确认
  3. 预计恢复时间：45分钟（参考历史同类缺陷）
关联事件：近30天该断路器操作电流异常记录3次（详见附件）
```

### 4.3 按监控意图自动配置呈现视图

运维人员无需手动配置监控大屏与告警规则，只需以自然语言描述监控意图：

> "我需要监控地下厂房所有旋转设备的振动趋势，一旦出现异常趋势提前预警"

平台自动完成：
- 从各接入系统能力中检索振动相关数据能力
- 建立采集策略与趋势分析模型
- 生成对应的监控视图卡片并推送至运维大屏

---

## 五、接入设备分类与适配策略

### 5.1 设备分类矩阵

| 设备类别 | 典型设备 | 主要协议 | 适配优先级 | 采集模式 |
|----------|----------|----------|------------|----------|
| **电力专业设备** | 保护装置、测控单元、PMU、故障录波器 | IEC61850、IEC104 | 高 | 事件订阅+定时轮询 |
| **变电站自动化** | RTU、监控后台、五防系统 | OPC-UA、Modbus | 高 | 定时轮询 |
| **手持IoT设备** | 智能安全帽、手持巡检仪、穿戴设备 | MQTT、HTTP | 中 | 事件驱动 |
| **人员管理** | 门禁、人脸识别、定位标签 | HTTP/SDK | 中 | 事件驱动 |
| **视频与AI摄像** | 高清摄像机、AI视频分析盒子 | GB/T 28181、RTSP | 中 | 流式+元数据 |
| **环境传感器** | 水位计、气象站、渗压计、振动计 | Modbus、NB-IoT | 低 | 定时轮询 |
| **施工机械** | TBM、塔吊、混凝土泵车 | 私有协议/MQTT | 低 | 定时轮询 |

### 5.2 接入友好性分级要求

对接入系统按配合程度进行分级管理：

- **A级（强制）**：提供标准能力声明文档（CapabilityManifest），支持平台主动拉取
- **B级（推荐）**：支持标准协议（IEC61850/OPC-UA/MQTT），平台可自动解析
- **C级（兼容）**：仅提供私有协议或文档，由平台AI解析接口文档后生成适配器，需厂商配合联调
- **D级（降级）**：完全不配合对接，仅接收其推送的Syslog/Trap类告警，数据价值有限

---

## 六、数据治理：最小必要数据原则

### 6.1 数据分层模型

```
┌──────────────────────────────────────────┐
│  L3 应用层数据  │ 报表、KPI、AI分析结论   │  永久保存
├──────────────────────────────────────────┤
│  L2 关键运营数据│ AI提炼后的必要字段      │  保存3~5年
├──────────────────────────────────────────┤
│  L1 原始采集数据│ 全量原始数据            │  保存30天后压缩
└──────────────────────────────────────────┘
```

### 6.2 AI提炼策略

LLM根据以下维度判断字段是否属于"必要数据"：

- **故障可观测性**：该字段是否有助于判断设备状态正常/异常
- **根因可追溯性**：该字段是否有助于定位故障根因
- **趋势预测价值**：该字段是否反映设备劣化趋势
- **合规必要性**：该字段是否属于安全规程要求记录的数据

不满足上述任一维度的字段，平台默认不采集，显著降低存储与处理成本。

---

## 七、平台工程实用性设计要求

### 7.1 去冗余设计

- 所有功能页面以"运维人员实际使用频率"为依据，低频功能折叠或移除
- 告警中心默认仅展示当前活跃根因事件，原始告警详情按需展开
- 报表系统基于LLM自动生成运营简报，替代人工填报，每日自动推送

### 7.2 快速上线机制

- 新系统接入从能力声明提交到完成首次数据采集，目标周期**≤1个工作日**
- 平台提供能力声明在线编辑器，厂商可自助完成能力描述，降低对接沟通成本
- 提供沙箱环境，厂商可在不影响生产的前提下自验证接入效果

### 7.3 运维友好性

- 支持自然语言查询：运维人员可直接输入"上周哪台设备告警最多"，无需学习查询语法
- 支持移动端告警推送与处置闭环，现场人员扫码关联工单
- 提供AI辅助运维手册：根据设备类型和故障类型自动推送处置步骤

---

## 八、安全与可靠性设计

### 8.1 网络安全分区

- 平台严格遵循电力监控系统安全防护规定，生产控制区与管理信息区物理隔离
- 边缘级节点与中心级之间通过加密专网通信，禁止走公共互联网
- 能力声明接口访问采用双向证书认证，防止非授权系统接入

### 8.2 高可用设计

- 中心级采用双活部署，单节点故障不影响全局服务
- 边缘级支持离岛运行模式：断开与中心级连接后，本地告警、联动控制、数据存储功能不受影响
- 数据采集链路具备断网续传能力，网络恢复后自动补传缺失数据

---

## 九、分阶段实施路线

| 阶段 | 时间参考 | 工作重点 | 交付物 |
|------|----------|----------|--------|
| **第一阶段：基础平台建设** | 筹建期 | 部署两级平台基础设施，建立能力声明规范，完成核心电力系统接入 | 平台上线 + A级系统全接入 |
| **第二阶段：AI适配引擎上线** | 施工初期 | 上线LLM采集适配引擎，完成手持设备、视频、IoT类系统接入 | AI适配引擎 + B/C级系统接入 |
| **第三阶段：智能分析能力建设** | 施工高峰期 | 上线根因分析引擎，建立全站拓扑知识图谱，完善监控意图配置 | 根因分析模块 + 告警压缩上线 |
| **第四阶段：持续优化** | 运营期 | 基于真实运营数据持续训练优化模型，扩展预测性维护能力 | 预测维护模块 + 运营报告体系 |

---

## 十、核心创新点总结

1. **能力声明式接入**：以类MCP协议替代逐一调测，接入效率提升10倍以上
2. **LLM驱动的最小必要数据提炼**：有效数据密度从10%提升至90%以上
3. **自动数据模型与接口生成**：消除DBA和开发人员的重复性适配工作
4. **AI根因分析替代告警轰炸**：运维人员关注点从数百条告警压缩至个位数事件
5. **监控意图驱动的视图自动生成**：运维人员无需手动配置，描述需求即得结果
6. **两级架构的边缘自治**：变电站侧低时延响应与中心侧全局分析有机结合

---

*本文档为平台设计要点纲要，各模块详细设计规格另行输出。*
